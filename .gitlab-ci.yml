stages:
    - run

Run Benchmark:
    stage: run
    tags:
        - zebrain
    script:
        - docker-compose up locator_benchmark
    when: manual
    
Run Viz:
    stage: run
    tags:
        - wehave
    script:
        - docker-compose up viz
    when: manual

