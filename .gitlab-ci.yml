stages:
    - run

Run Benchmark:
    stage: run
    tags:
        - zebrain
    script:
        - docker-compose up -d locator_benchmark
    when: manual
    
Run Viz:
    stage: run
    tags:
        - preprod3
    script:
        - docker-compose down
        - docker-compose up -d --build viz
    when: manual

