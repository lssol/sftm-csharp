stages:
    - run

Run Benchmark:
    stage: run
    tags:
        - zebrain
    script:
        - docker-compose up locator_benchmark
Run Viz:
    stage: run
    tags:
        - wehave
    script:
        - docker-compose up viz

